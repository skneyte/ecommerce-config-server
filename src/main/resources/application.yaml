spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://${GITHUB_USER}:${GITHUB_TOKEN}@github.com/skneyte/microservices-config-data.git
          clone-on-start: true
          default-label: main

server:
  port: ${PORT:8888}
